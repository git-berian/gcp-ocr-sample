services:
  functions:
    build:
      context: ../../..
      dockerfile: packages/functions/docker/Dockerfile
    env_file: ../../../.env
    volumes:
      - ../../..:/app
      - node_modules:/app/node_modules
    working_dir: /app

volumes:
  node_modules:
