name: docai-web

services:
  web:
    build:
      context: ../../..
      dockerfile: packages/web/docker/Dockerfile
    env_file: ../../../.env
    ports:
      - "5173:5173"
    volumes:
      - ../../..:/app
      - node_modules:/app/node_modules
    working_dir: /app

volumes:
  node_modules:
